name: Compressed Size

on: 
  pull_request:

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - name: Checkout the repo
      uses: actions/checkout@v2
      
    - uses: preactjs/compressed-size-action@v2
      with:
        pattern: "index.{js,d.ts}"
